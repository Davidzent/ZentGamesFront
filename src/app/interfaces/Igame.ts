export interface IGame {
    gameid?: number; //unique id mainly used for form request
    name?: string; //sometimes we might not want to have this like when displaying purchased orders
    publisher?: string; //sometimes we might not want to have this like when displaying purchased orders
}
